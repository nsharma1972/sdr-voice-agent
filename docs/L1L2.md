# Karthik's L1/L2 work — index (Issues #5–#9)

This work is built **on top of Naren's existing engine** (`src/signals/`,
`src/qualify/`, `src/db.py`, portal). It adds the missing pieces, matching his
conventions — no competing rewrite.

## What was added / changed

| Issue | File(s) | Notes |
|---|---|---|
| #7 Hiring signal (S5) | `src/signals/linkedin.py` (new) | `fetch_hiring_signals()`, zero-key Google News RSS backend; LinkedIn/Adzuna upgrade hooks. Wired into `pipeline.py` as `LinkedIn_Jobs`. |
| #6 Industry packs | `src/signals/queries.py` (new) | `INDUSTRY_PACK=fintech\|healthtech\|saas` → vertical news + hiring queries. Wired into `pipeline.py`. |
| #6/#7 wiring | `src/qualify/pipeline.py` (edited) | Adds the hiring source + pack queries; returns `industry_pack`. |
| #5 ICP | `docs/ICP.md` | Personas, verticals, signal→buyer map, scoring/tiers. |
| #8 Templates | `docs/message-templates.md` | Email/LinkedIn/voicemail per signal type (S1–S7). |
| #9 Demo | `docs/DEMO.md` | 3-min script + E2E runbook + acceptance log. |

## Run

```bash
pip install aiosqlite httpx
INDUSTRY_PACK=fintech make dev
curl -X POST "localhost:8000/signals/refresh?days=120"   # signal_counts includes LinkedIn_Jobs (S5)
curl "localhost:8000/leads?tier=CONTACT"
```

## Note on the earlier scaffold

An earlier exploratory build (campaign-pack approach: `src/models.py`,
`src/campaign.py`, `src/signals/jobs.py`, `src/qualify/api.py`, `campaigns/*.yaml`,
`scripts/smoke_test.py`) is **superseded** by this Naren-aligned version and should
be removed — see the cleanup command in the chat. Keeping both would duplicate the
qualification layer.
